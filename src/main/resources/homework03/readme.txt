ДЗ#3

Репозиторий:
https://github.com/alexei-28/Algorithms
Язык программирования: Java 1.8

- Скачайте проект и импортните в IntellIJ IDEA - https://www.jetbrains.com/ru-ru/idea/
- Точка входа приложения для ДЗ#3 - Main

Сделал 4 задачи из домашнего задания: /src/main/resources/homework03

  0. Сравнение операций удвление (за рамками ДЗ)
   Исходники:
     MultipleManyTimes.java

   Для запуска теста (проверка длины строки) нужно раскомментировать только строку в файле
     Main.java:

       new MultipleManyTimes();

   Сравнительная таблица результатов: src/main/resources/homework03/comparison_table.txt

   Время выполнения: 1 час
  -----------------------------------------------------------------------------------------------

  1. Король решил прогуляться по пустынной шахматной доске.
  Исходники:
  BitBoardKing.java

      Для запуска теста (проверка длины строки) нужно раскомментировать только строку в файле
      Main.java:

        Tester tester = new Tester(new File(pathBase + File.separator + "1.Bitboard - Король"), new BitBoardKing());

      Время выполнения - 4 часа

      Оценка: +2 байт
  -----------------------------------------------------------------------------------------------
  2. Шахматный конь решил пробежаться по шахматной доске.
      Исходники:
      BitBoardKnight.java

      Для запуска теста (проверка длины строки) нужно раскомментировать только строку в файле
      Main.java:

        Tester tester = new Tester(new File(pathBase + File.separator + "2.Bitboard - Конь"), new BitBoardKnight());

    Время выполнения - 4 часа

    Оценка: +2 байта
  -----------------------------------------------------------------------------------------------
  3. Дано расположение шахматных фигур на доске в FEN-нотации (первая часть fen-кода).
     Перевести её в Bitboard Board-Definition форма

      Исходники:
      BitBoardFEN.java

       Для запуска теста (проверка длины строки) нужно раскомментировать только строку в файле
       Main.java:

        Tester tester = new Tester(new File(pathBase + File.separator + "3.Bitboard - FEN"), new BitBoardFEN());

      Время выполнения - 6 часов

      Оценка: +2 байта
   -----------------------------------------------------------------------------------------------
   4. Определить битовую маску возможных ходов белых фигур - ладьи, слона и ферзя.

         Исходники:
         BitBoardTruckers.java

          Для запуска теста (проверка длины строки) нужно раскомментировать только строку в файле
          Main.java:

              Tester tester = new Tester(new File(pathBase + File.separator + "4.Bitboard - Дальнобойщики"), new BitBoardTruckers());

         Время выполнения - 12 часов
         Оценка: +4 байта
   -----------------------------------------------------------------------------------------------

   Итого: +10 байт.